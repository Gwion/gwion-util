language: c


os:
  - linux

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-8

before_script: if [ "$CC" = "gcc" ]; then export CC=gcc-8; fi

script: make
